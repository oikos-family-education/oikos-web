{
  "name": "oikos",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:coverage": "turbo test:coverage",
    "type-check": "turbo type-check",
    "db:doctor": "./scripts/db-doctor.sh",
    "db:reset": "./scripts/db-reset.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
